# -*- mode: shell-script -*-
# vi: set ft=shell-script :


# custom DD consul helpers
if [ -f /etc/profile.d/consul.sh ]; then
    . /etc/profile.d/consul.sh
fi

# custom tools
export PATH=~/.bin:$PATH

export EDITOR=emacs
export KNIFE_HOME=/etc/chef
